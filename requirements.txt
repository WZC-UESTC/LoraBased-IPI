# ============================================================
# LoRA-IPI 依赖 (版本锁定, 兼容 Python 3.10 + CUDA 12.x)
# ============================================================
# 安装: pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
# ============================================================

# --- PyTorch (先单独装) ---
# pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cu121
torch==2.5.1

# --- Transformers 生态 (版本锁定, 互相兼容) ---
transformers==4.46.3
peft==0.13.2
accelerate==1.2.1
trl==0.11.4
tokenizers>=0.20,<0.21
datasets>=2.20
bitsandbytes>=0.44.0

# --- ModelScope 下载模型 ---
modelscope>=1.20.0

# --- 数据处理 ---
pyyaml>=6.0
numpy>=1.26
tqdm>=4.65

# --- 评估分析 ---
scikit-learn>=1.5
scipy>=1.13
matplotlib>=3.8

# --- Agent 仿真 (评估用) ---
openai>=1.40
requests>=2.32

# --- 命令行工具 ---
click>=8.1
loguru>=0.7
rich>=13.7
